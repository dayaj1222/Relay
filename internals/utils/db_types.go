// Package utils provides shared helpers used by multiple packages.
package utils

// Intentionally empty or minimal — JSONB was removed in favor of
// json.RawMessage used directly by consumers. Add shared utilities here.